{
  "manifest_version": 2,
  "name": "Send Tag Value to Flask",
  "version": "1.1",
  "description": "Reads a value from an HTML tag and sends it every second to a local Flask server.",
  "permissions": [
    "activeTab",
    "http://127.0.0.1:5000/*"
  ],
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content-script.js"],
      "run_at": "document_idle"
    }
  ]
}
